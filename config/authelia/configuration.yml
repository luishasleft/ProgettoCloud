host: 0.0.0.0
port: 9091

jwt_secret: unsegretoforte

authentication_backend:
  file:
    path: /config/users_database.yml

session:
  secret: sessionsecret
  cookies:
    - name: authelia_session

storage:
  local:
    path: /config/db.sqlite3

access_control:
  default_policy: deny
  rules:
    - domain: localhost
      policy: one_factor

notifier:
  filesystem:
    filename: /config/notification.txt
